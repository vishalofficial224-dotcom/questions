

//see if at least one element in a collection satisfies a condition(you don't care how many-just yes or no).
const numbers = [1, 3,5,6,7,8];

const isEven = numbers.some(x => x%2 === 0);

console.log(isEven)


// count matching elements => how many? (numbers)
// check if any matches => is there at least one?(true/false);




